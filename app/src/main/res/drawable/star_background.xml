<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background Circle -->
    <item>
        <shape
            android:shape="oval">
            <solid android:color="@color/quran_background"/> <!-- Adjust color as needed -->
        </shape>
    </item>

    <!-- Foreground Star -->
    <item
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"
        android:bottom="10dp">
        <rotate
            android:fromDegrees="45"
            android:pivotX="50%"
            android:pivotY="50%">
            <shape
                android:shape="rectangle">
                <solid android:color="#9D4D2C"/> <!-- Adjust color as needed -->
            </shape>
        </rotate>
    </item>

    <item
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"
        android:bottom="10dp">
        <rotate
            android:fromDegrees="90"
            android:pivotX="50%"
            android:pivotY="50%">
            <shape
                android:shape="rectangle">
                <solid android:color="#9D4D2C"/> <!-- Adjust color as needed -->
            </shape>
        </rotate>
    </item>

    <!-- Background Circle -->
    <item>
        <shape
            android:shape="oval">
            <solid android:color="@color/quran_background"/> <!-- Adjust color as needed -->
        </shape>
    </item>

    <!-- Foreground Star -->
    <item
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"
        android:bottom="10dp">
        <rotate
            android:fromDegrees="45"
            android:pivotX="50%"
            android:pivotY="50%">
            <shape
                android:shape="rectangle">
                <solid android:color="#9D4D2C"/> <!-- Adjust color as needed -->
            </shape>
        </rotate>
    </item>

    <item
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"
        android:bottom="10dp">
        <rotate
            android:fromDegrees="90"
            android:pivotX="50%"
            android:pivotY="50%">
            <shape
                android:shape="rectangle">
                <solid android:color="#9D4D2C"/> <!-- Adjust color as needed -->
            </shape>
        </rotate>
    </item>

</layer-list>
